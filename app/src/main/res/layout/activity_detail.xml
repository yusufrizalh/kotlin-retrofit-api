<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    tools:context=".DetailActivity">

    <EditText
        android:id="@+id/edit_course_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:hint="Please enter course name"
        android:inputType="text"
        android:minHeight="60dp" />

    <EditText
        android:id="@+id/edit_course_duration"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:hint="Please enter course duration"
        android:inputType="text"
        android:minHeight="60dp" />

    <EditText
        android:id="@+id/edit_course_price"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:hint="Please enter course price"
        android:inputType="text"
        android:minHeight="60dp" />

    <EditText
        android:id="@+id/edit_course_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:hint="Please enter course description"
        android:inputType="text"
        android:minHeight="60dp" />

    <Button
        android:id="@+id/button_update"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Update Course"
        android:textAllCaps="true"
        app:cornerRadius="150dp" />

</LinearLayout>